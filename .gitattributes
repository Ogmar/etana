# Normalize line endings: store text files with LF in the repository, and
# check them out with LF on every platform. Prevents phantom "whole file
# changed" diffs when contributors are on different operating systems.
* text=auto eol=lf

# Explicitly treat known binary types as binary (never touch their bytes).
*.png binary
*.jpg binary
*.pdf binary
*.zip binary
