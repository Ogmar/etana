# Copy to .env.local and adjust for a deployed frontend.
# Leave unset locally: the dev server proxies /api to the Django server
# (see vite.config.ts), so no absolute URL is needed.
# VITE_API_BASE_URL=https://your-api.up.railway.app/api
